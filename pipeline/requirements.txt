# --- Core data ---
# yfinance pinned to a recent release; curl_cffi bypasses Yahoo's bot detection
# that blocks data-center IPs (GitHub Actions runners share these).
yfinance>=0.2.55,<0.3
curl_cffi>=0.7.0
pandas==2.2.3
numpy==2.0.2

# --- Technical analysis ---
ta==0.11.0

# --- HTTP ---
# httpx pinned to <0.28 because supabase 2.10 requires httpx<0.28
httpx==0.27.2
requests==2.32.3

# --- Database ---
supabase==2.10.0
postgrest==0.18.0

# --- NLP via HuggingFace Inference Providers ---
# >=0.28 includes the Inference Providers routing that replaced the legacy
# api-inference.huggingface.co URL pattern (which now 404s for many models).
# Still used by the LLM-side summarizer + verdict reasoner; FinBERT moved
# to local execution (see transformers/torch below) to escape the router's
# rate-limit / cold-start tax on the hot sentiment path.
huggingface-hub>=0.28.0,<0.31

# --- Local FinBERT (sentiment) ---
# transformers loads the model + tokenizer; torch runs the forward pass.
# CPU-only torch wheel keeps install size sane (~200 MB vs 800 MB for CUDA).
# Install via:
#   pip install --extra-index-url https://download.pytorch.org/whl/cpu -r requirements.txt
# (the GH Actions workflow handles this; locally you may want a venv).
transformers>=4.40,<5
torch>=2.2,<3

# --- Reddit ---
praw==7.8.1

# --- Market calendar (trading days) ---
pandas-market-calendars==4.5.1

# --- Observability ---
sentry-sdk==2.20.0

# --- Config ---
python-dotenv==1.0.1

# --- SEC filings ---
beautifulsoup4==4.12.3
lxml==5.3.0

# --- Tests ---
pytest==8.3.4
pytest-asyncio==0.25.2
respx==0.22.0       # httpx mock
